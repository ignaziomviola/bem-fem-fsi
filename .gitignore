__pycache__/
*.py[cod]

# Sample meshes and profiles: regenerate with make_thick_sample_inputs.py
*.npz
*.csv

# Figures
*.png
*.pdf

# ...except the manuscript's, which are committed so that it compiles without a
# twenty-five-minute run. docs/paper/make_figures.py regenerates them.
!docs/paper/figures/*.pdf
!docs/paper/figures/*.png

.DS_Store
.venv/
runs_ramp.log
runs_ramp_g02.log
runs/**/state.pkl
runs_ramp*.log
